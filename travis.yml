sudo: required
services:
   - docker

before_install:
   - docker build -t PrabhuKiran1229 /docker-react -f Dockerfile.dev .

script:
   - docker run PrabhuKiran1229 /docker-react npm run test -- --coverage
   